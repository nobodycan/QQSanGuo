# Phase 03 Implementation Plan

1. Add failing static-gate fixtures for a seventh Autoload, direct scene switch, and unsafe APIs.
2. Add the baseline ledger and PowerShell gate; make the resource lane execute it.
3. Add `ContentRegistry`, `EventBus`, and `AudioManager` Autoload shells plus unit tests.
4. Register the three services in `project.godot` without removing legacy Autoloads.
5. Run resource and unit lanes, update README and Release Notes, commit exact files, and push.
